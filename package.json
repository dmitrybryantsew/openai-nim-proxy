{
  "name": "openai-nim-proxy",
  "version": "1.0.0",
  "description": "OpenAI compatible proxy for NVIDIA NIM API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/model-registry.js && node --check src/chat-store.js && node --check src/responses.js && node --check src/key-pool.js && node --check src/debug-log.js && node --check public/app.js && node --check public/tts.js && node --check public/g4f.js && node --check scripts/tool-call-probe.js && node --check scripts/hermes-tool-probe.js && python -c \"import ast, pathlib; ast.parse(pathlib.Path('scripts/kittentts-synthesize.py').read_text())\"",
    "smoke": "bash scripts/check.sh",
    "probe:tools": "node scripts/tool-call-probe.js",
    "probe:hermes": "node scripts/hermes-tool-probe.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "express-session": "^1.19.0",
    "google-auth-library": "^11.0.1",
    "sqlite3": "^6.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
