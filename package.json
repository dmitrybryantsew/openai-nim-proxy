{
  "name": "openai-nim-proxy",
  "version": "1.0.0",
  "description": "OpenAI compatible proxy for NVIDIA NIM API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/model-registry.js && node --check src/chat-store.js && node --check public/app.js && node --check public/tts.js && node --check public/g4f.js && node --check scripts/tool-call-probe.js && node --check scripts/hermes-tool-probe.js && python -c \"import ast, pathlib; ast.parse(pathlib.Path('scripts/kittentts-synthesize.py').read_text())\"",
    "smoke": "bash scripts/check.sh",
    "probe:tools": "node scripts/tool-call-probe.js",
    "probe:hermes": "node scripts/hermes-tool-probe.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=20"
  }
}
